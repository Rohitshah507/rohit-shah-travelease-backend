<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Your Email</title>
    <style>
        /* General Reset */
        body, table, td, a { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
        body {
            height: 100% !important;
            margin: 0 !important;
            padding: 0 !important;
            width: 100% !important;
            /* Modern Linear Gradient Background */
            background: linear-gradient(135deg, #fce4ec 0%, #f8bbd0 100%);
            background-color: #fce4ec; /* Fallback */
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        .wrapper {
            padding: 60px 20px;
        }

.container {
    max-width: 500px;
    margin: 0 auto;
    background-color: #ffffff;
    text-align: center;
    
    /* 1. Set border-radius */
    border-radius: 16px;
    
    /* 2. Create the gradient border effect */
    border: 1px solid transparent; /* Transparent base border */
    background-image: linear-gradient(#fff, #fff), 
                      linear-gradient(to right, #ffffff, #ffffff); 
    background-origin: border-box;
    background-clip: content-box, border-box;
    
    /* 3. Add shadow and handle overflow */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    overflow: hidden;
}
        .top-accent {
            height: 6px;
            background-color: #E31E24; /* Red accent from your image */
            width: 100%;
        }

        .content {
            padding: 40px 30px;
        }

        .logo {
            width: 60px;
            height: auto;
            margin-bottom: 25px;
        }

        h1 {
            color: #333333;
            font-size: 24px;
            font-weight: 600;
            margin: 0 0 15px 0;
            border-bottom: 1px solid #f0f0f0;
            padding-bottom: 20px;
        }

        p {
            color: #555555;
            font-size: 15px;
            line-height: 1.6;
            margin: 15px 0;
        }

        .btn-container {
            margin: 35px 0;
        }

        .btn {
            background-color: #3B4FB2; /* Brand Blue */
            color: #ffffff !important;
            text-decoration: none;
            padding: 14px 35px;
            border-radius: 8px; /* Rounded button corners */
            font-size: 15px;
            font-weight: bold;
            display: inline-block;
            transition: background 0.3s ease;
        }

        .link-text {
            font-size: 12px;
            color: #888888;
            margin-top: 30px;
        }

        .footer-link {
            display: block;
            color: #3B4FB2;
            font-size: 12px;
            text-decoration: none;
            word-break: break-all;
            margin-top: 10px;
            padding: 10px;
            background: #f9f9f9;
            border-radius: 4px;
        }

        .footer {
            font-size: 11px;
            color: #aaaaaa;
            margin-top: 40px;
            border-top: 1px solid #f0f0f0;
            padding-top: 20px;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="container">
            <div class="top-accent"></div>
            
            <div class="content">
                <img src="https://cdn-icons-png.flaticon.com/512/3670/3670151.png" alt="Logo" class="logo">
                
                <h1>Email Verification</h1>
                
                <p>Welcome! To complete your registration and verify your email address, please click the button below.</p>
                <p style="font-size: 13px; color: #888;">If you did not create an account, you can safely ignore this email.</p>
                
                <div class="btn-container">
                    <a href="http://localhost:3000/api/v1/auth/verify-email/{{token}}" class="btn">Verify Your Email Address</a>
                </div>
                
                <p class="link-text">Having trouble with the button? Copy and paste this link:</p>
                <a href="http://localhost:3000/api/v1/auth/verify-email/{{token}}" class="footer-link">
                    Have a Nice Day
                </a>
                
                <div class="footer">
                    Questions? Contact <a href="#" style="color: #777; text-decoration: underline;">Minute School Support</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>